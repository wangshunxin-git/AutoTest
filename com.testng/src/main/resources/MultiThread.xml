<?xml version="1.0" encoding="UTF-8" ?>

    <suite name="thread" parallel="methods" thread-count="3">
        <!--
        tests级别：不同的test tag 下的用例可以在不同的线程下执行
                  相同的test tag  下的用例只能在同一个线程下执行

        class级别：相同的 class tag下的用例在同一个线程下执行
                  不同的 class tag下的用例可以在不同的线程下执行
        methods级别：所有用例都可以在不同的线程下去执行

        thread-count:代表了最大并发线程数

        xml文件配置这种方式不能指定线程池，只有方法上才可以指定线程池
        -->
        <test  name="Demo01">
            <classes name="d1">

                <class name="com.course.testNG.multiThread.MultiThreadOnXml"/>
                <class name="com.course.testNG.BasicAnnotation"/>
                <class name="com.course.testNG.multiThread.MultiThreadOnXml"/>
            </classes>

            <classes name="d2">

                <class name="com.course.testNG.multiThread.MultiThreadOnXml"/>
                <class name="com.course.testNG.BasicAnnotation"/>
                <class name="com.course.testNG.multiThread.MultiThreadOnXml"/>
            </classes>
        </test>

        <test name="Demo02">
            <classes name="d3">

                <class name="com.course.testNG.BasicAnnotation"/>
            </classes>

        </test>
    </suite>